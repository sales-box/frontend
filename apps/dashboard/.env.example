VITE_API_BASE_URL=https://salesbox.dev
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Where the packed extension .zip is hosted. Leave blank until it exists —
# the download page shows an honest notice instead of a dead button.
VITE_EXTENSION_DOWNLOAD_URL=
VITE_EXTENSION_VERSION=
